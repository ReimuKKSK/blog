{
  "name": "shit",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "deploy": "bash deploy.sh",
    "dev": "vuepress dev docs",
    "build": "set port=9000 && vuepress build docs",
    "dev-baka": "SET NODE_OPTIONS=--openssl-legacy-provider && vuepress dev docs",
    "build-baka": "SET NODE_OPTIONS=--openssl-legacy-provider && vuepress build docs",
    "deploy-baka": "SET NODE_OPTIONS=--openssl-legacy-provider && bash deploy.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vuepress": "^1.9.7"
  }
}
